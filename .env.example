# Database (SQLite for local dev — change to postgres:// for production)
DATABASE_URL="file:./prisma/dev.db"

# Meta WhatsApp Cloud API
WHATSAPP_TOKEN=           # System User Access Token from Meta Developer Portal
PHONE_NUMBER_ID=          # WhatsApp Business phone number ID from Meta Developer Portal
VERIFY_TOKEN=             # Any secret string you choose for webhook verification

# Anthropic (Claude AI)
ANTHROPIC_API_KEY=        # Your Anthropic API key from console.anthropic.com

# Business owner WhatsApp number (for human handoff alerts)
OWNER_PHONE=              # E.g. 447911123456 (international format, no + or spaces)

# Google Calendar (Service Account)
# Setup: https://console.cloud.google.com → Enable Calendar API → Create Service Account → Share calendar with service account email
GOOGLE_SERVICE_ACCOUNT_EMAIL=   # e.g. bot@my-project.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY=             # Private key from service account JSON (paste the full -----BEGIN PRIVATE KEY----- block)
GOOGLE_CALENDAR_ID=             # Calendar ID from Google Calendar Settings → Integrate calendar (e.g. owner@gmail.com)

# Server
PORT=3000
