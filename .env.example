# Database (SQLite for local dev — change to postgres:// for production)
DATABASE_URL="file:./prisma/dev.db"

# Anthropic (Claude AI) — shared across all tenants
ANTHROPIC_API_KEY=        # Your Anthropic API key from console.anthropic.com

# Webhook verification (shared across all tenants)
VERIFY_TOKEN=             # Any secret string you choose for webhook verification

# JWT secret for dashboard auth
JWT_SECRET=               # A random string for signing JWT tokens (e.g. openssl rand -hex 32)

# Server
PORT=3000

# Tenant-specific config (WhatsApp credentials, Google Calendar, owner phone)
# is now stored in the database per tenant — see POST /api/auth/register
